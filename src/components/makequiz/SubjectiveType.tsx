import { showText } from '@/app/action';

const SubjectiveType = () => {
  return <textarea className="resize-none w-full h-20" name="answer" placeholder="답변을 입력해주세요"></textarea>;
};

export default SubjectiveType;
