export const BALLSIZE = 4;

export const MINDIFFERENCE = 20;

export const DAY_OF_FETCH_ALGORITHM = 3;

export const NUM_OF_FETCH_ALGORITHM = 10;

export const tree_balls = Array.from({ length: 10 }).map((_, idx) => `/ball${idx + 1}.png`);
